<section id="contact" class="py-20">
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <div class="grid lg:grid-cols-2 gap-10 items-start">
      <div>
        <h2 class="text-3xl font-bold text-slate-900">Parlons de votre projet</h2>
        <p class="mt-2 text-slate-600">Décrivez vos besoins et nous revenons vers vous sous 24h ouvrées.</p>

        <div class="mt-6 rounded-2xl bg-white ring-1 ring-slate-200 p-6 shadow-sm">
          <form name="contact" method="post" data-netlify="true" netlify-honeypot="bot-field">
            <input type="hidden" name="form-name" value="contact" />
            <p class="hidden">
              <label for="bot-field">Ne pas remplir</label>
              <input id="bot-field" name="bot-field" />
            </p>

            <div class="grid sm:grid-cols-2 gap-4">
              <div>
                <label for="name" class="block text-sm text-slate-600">Nom</label>
                <input
                  id="name"
                  name="name"
                  required
                  autocomplete="name"
                  class="mt-1 w-full rounded-xl border border-slate-300 px-3 py-2 focus:outline-none focus:ring-2 focus:ring-slate-900"
                />
              </div>

              <div>
                <label for="email" class="block text-sm text-slate-600">Email</label>
                <input
                  id="email"
                  type="email"
                  name="email"
                  required
                  autocomplete="email"
                  class="mt-1 w-full rounded-xl border border-slate-300 px-3 py-2 focus:outline-none focus:ring-2 focus:ring-slate-900"
                />
              </div>
            </div>

            <div class="mt-4">
              <label for="message" class="block text-sm text-slate-600">Message</label>
              <textarea
                id="message"
                name="message"
                rows="5"
                class="mt-1 w-full rounded-xl border border-slate-300 px-3 py-2 focus:outline-none focus:ring-2 focus:ring-slate-900"
              ></textarea>
            </div>

            <button
              type="submit"
              class="mt-6 inline-flex items-center rounded-xl bg-slate-900 text-white px-5 py-3 font-medium hover:bg-slate-800"
            >
              Envoyer
            </button>
          </form>
        </div>
      </div>

      <div class="rounded-2xl bg-white ring-1 ring-slate-200 p-6 shadow-sm">
        <h3 class="font-semibold text-slate-900">Coordonnées</h3>
        <ul class="mt-3 space-y-2 text-slate-600">
          <li>📍 Abidjan, Côte d’Ivoire</li>
          <li>📞 +225 07 79 506 051</li>
          <li>✉️ contact@brandname.com</li>
        </ul>
        <div class="mt-6 aspect-video rounded-xl overflow-hidden">
          <iframe
            class="h-full w-full"
            title="maps"
            src="https://maps.google.com/maps?q=Abidjan&t=&z=12&ie=UTF8&iwloc=&output=embed"
            loading="lazy"
          ></iframe>
        </div>
      </div>
    </div>
  </div>
</section>
